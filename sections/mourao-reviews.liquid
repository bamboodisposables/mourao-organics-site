<section id="reviews" class="mourao-section mourao-section--spacious">
  <div class="page-width">
    <div class="mourao-section-header">
      <p class="mourao-eyebrow">Reviews</p>
      <h2 class="mourao-section-title">{{ section.settings.title }}</h2>
      <p class="mourao-section-copy">Geen overdreven beloftes, maar reacties van mensen die vooral rust, zachtheid en eenvoud zoeken in hun verzorging.</p>
    </div>

    <div class="mourao-grid mourao-grid--reviews">
      {% for block in section.blocks %}
        <div {{ block.shopify_attributes }}>
          {% render 'mourao-review-card',
            author: block.settings.author,
            context: block.settings.context,
            text: block.settings.text,
            accent_index: forloop.index
          %}
        </div>
      {% endfor %}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Mourao reviews",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Title",
      "default": "Mensen blijven terugkomen voor de rust"
    }
  ],
  "blocks": [
    {
      "type": "review",
      "name": "Review",
      "settings": [
        {
          "type": "text",
          "id": "author",
          "label": "Author",
          "default": "Sanne"
        },
        {
          "type": "text",
          "id": "context",
          "label": "Context",
          "default": "Droge, gevoelige huid"
        },
        {
          "type": "textarea",
          "id": "text",
          "label": "Text",
          "default": "Placeholder reviewtekst."
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Mourao reviews"
    }
  ]
}
{% endschema %}
